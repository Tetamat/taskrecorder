specVersion: "v1beta"
serviceId: "TaskRecorder"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "TaskRecorder-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
